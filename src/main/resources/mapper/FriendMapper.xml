<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wetalk.modules.social.mapper.FriendMapper">
    <select id="findFriendIds" resultType="java.lang.String">
        SELECT friend_id FROM friend_relation
        WHERE user_id = #{userId} AND status = 1 AND deleted_at IS NULL
    </select>
    <select id="findByUserAndFriend" resultType="com.wetalk.modules.social.entity.FriendRelation">
        SELECT * FROM friend_relation
        WHERE user_id = #{userId} AND friend_id = #{friendId} AND deleted_at IS NULL
    </select>
</mapper>
